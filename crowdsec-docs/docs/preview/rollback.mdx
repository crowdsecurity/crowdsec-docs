---
id: rollback
title: Rollback to previous version
---

If you want to rollback to a previous version of the security engine, you can do so by running the following commands:

```bash
sudo systemctl stop crowdsec
```