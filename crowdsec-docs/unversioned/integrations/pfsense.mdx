---
id: pfsense
title: pfSense
---

import ThemedImage from "@theme/ThemedImage";
import useBaseUrl from "@docusaurus/useBaseUrl";

The CrowdSec pfSense integration connects CrowdSec's hosted blocklist endpoint to your pfSense firewall.
In pfSense, you'll use **URL Table (IPs) aliases** to create aliases that periodically download and update IP lists from external URLs.

:::info
Ensure your pfSense version supports URL Table (IPs) aliases. If unsure, refer to the pfSense documentation or contact pfSense support.
:::

## Step 1 — Create the integration in the CrowdSec Console

In the [Integrations](https://app.crowdsec.net/blocklists/integrations) page, click **Connect** under the pfSense card.

<ThemedImage
    alt="pfSense Integration Card"
    sources={{
        light: useBaseUrl("/img/console_integrations_pfsense_card_light.png"),
        dark: useBaseUrl("/img/console_integrations_pfsense_card_dark.png"),
    }}
/>

Name the integration (must be unique to your account), then click **Create**.

<ThemedImage
    alt="pfSense Integration Creation Screen"
    sources={{
        light: useBaseUrl("/img/console_integrations_creation_light.png"),
        dark: useBaseUrl("/img/console_integrations_creation_dark.png"),
    }}
/>

:::warning
The credentials shown next are displayed **only once**. Copy them before closing this screen.
:::

<ThemedImage
    alt="pfSense Integration Credentials Screen"
    sources={{
        light: useBaseUrl("/img/console_integrations_generic_credentials_light.png"),
        dark: useBaseUrl("/img/console_integrations_generic_credentials_dark.png"),
    }}
/>

You now have an HTTPS endpoint and Basic Auth credentials to configure on your pfSense device.

## Step 2 — Configure pfSense

1. Create a URL Table (IPs) alias with a 1-day update frequency. Embed the credentials in the URL using Basic Auth:

```
https://<username>:<password>@admin.api.crowdsec.net/v1/integrations/<integration_id>/content
```

2. Verify the URL alias is resolving correctly.
3. Create a firewall rule to block IPs matching the alias.

Here is a walkthrough of the full pfSense configuration:

![](/img/pfsense.gif)

## Manage integration size limits with pagination

If you want to learn how to manage integration size limits with pagination, please refer to the [Managing integrations size limits with pagination](console/service_api/integrations.md#managing-integrations-size-limits-with-pagination) section.

## Next Steps

Subscribe to blocklists in the [Blocklist Catalog](console/blocklists/catalog.md) to populate your integration.
