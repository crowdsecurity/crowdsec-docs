---
id: console_hub
title: CrowdSec Hub
---

import ThemedImage from "@theme/ThemedImage";
import useBaseUrl from "@docusaurus/useBaseUrl";

# CrowdSec Hub

The [CrowdSec Hub](https://hub.crowdsec.net/) is a repository of ready-to-use configuration files.
Resources are maintained by both the CrowdSec team and the community.

## Hub Tour

The [Hub](https://hub.crowdsec.net/) is divided into sections to help you find the right configuration.

<ThemedImage
    alt="CrowdSec Hub"
    sources={{
        light: useBaseUrl("/img/console_hub_light.png"),
        dark: useBaseUrl("/img/console_hub_dark.png"),
    }}
/>

### Collections Tab

[Collections](/docs/next/log_processor/collections/intro) are sets of configurations that work together. For example, the [crowdsecurity/sshd collection](https://app.crowdsec.net/hub/author/crowdsecurity/collections/sshd) focuses on SSH attack patterns.

<ThemedImage
    alt="CrowdSec sshd collection"
    sources={{
        light: useBaseUrl("/img/console_sshd_collection_light.png"),
        dark: useBaseUrl("/img/console_sshd_collection_dark.png"),
    }}
/>

You can see the contents of a collection in the `Content` section.

<ThemedImage
    alt="CrowdSec sshd collection content"
    sources={{
        light: useBaseUrl("/img/console_sshd_collection_content_light.png"),
        dark: useBaseUrl("/img/console_sshd_collection_content_dark.png"),
    }}
/>

As shown above, the `sshd` collection includes a [parser](/docs/next/log_processor/parsers/intro) and [scenarios](/docs/next/log_processor/scenarios/intro) focused on brute-force attacks.

### Configurations Tab

The configurations tab holds individual files you can use with your CrowdSec setup. Each item includes tags to help you identify its purpose.

<ThemedImage
    alt="CrowdSec configurations"
    sources={{
        light: useBaseUrl("/img/console_configurations_light.png"),
        dark: useBaseUrl("/img/console_configurations_dark.png"),
    }}
/>

For example, [apache_log4j2_cve-2021-44228](https://app.crowdsec.net/hub/author/crowdsecurity/configurations/apache_log4j2_cve-2021-44228) is an `Attack scenario` designed to detect Log4j2 CVE-2021-44228 exploitation.

<ThemedImage
    alt="CrowdSec apache_log4j2_cve-2021-44228"
    sources={{
        light: useBaseUrl("/img/console_log4j_light.png"),
        dark: useBaseUrl("/img/console_log4j_dark.png"),
    }}
/>

Another example is [crowdsecurity/nginx-logs](https://app.crowdsec.net/hub/author/crowdsecurity/configurations/nginx-logs), a `Log parser` designed to parse Nginx logs.

<ThemedImage
    alt="CrowdSec nginx-logs"
    sources={{
        light: useBaseUrl("/img/console_nginx_logs_light.png"),
        dark: useBaseUrl("/img/console_nginx_logs_dark.png"),
    }}
/>

:::info
There are more configuration types than `Attack scenario` and `Log parser`, but these are the most common.
:::

### Bouncers Tab

:::info
The term `Bouncers` has been updated to `Remediation Components` in the [Taxonomy](https://www.crowdsec.net/blog/updating-crowdsec-naming-taxonomy).

Legacy items might still use the term `bouncers`. They mean the same thing.
:::

This tab contains [Remediation Components](/bouncers/intro.md) that enforce decisions made by the CrowdSec [Security Engine](/docs/next/intro#architecture).

<ThemedImage
    alt="CrowdSec bouncers"
    sources={{
        light: useBaseUrl("/img/console_bouncers_light.png"),
        dark: useBaseUrl("/img/console_bouncers_dark.png"),
    }}
/>

For example, [crowdsecurity/iptables](https://app.crowdsec.net/hub/author/crowdsecurity/remediation-components/iptables) blocks IP addresses using `iptables`.

:::info
Please note the download figures are solely from GitHub metrics and do not include downloads from other sources.
:::

### AppSec Configurations Tab

:::info
Since version `1.6.0`, CrowdSec includes the [AppSec Component](/docs/next/appsec/intro), which lets you run CrowdSec as a Web Application Firewall (WAF).
:::

[AppSec configurations](/docs/next/appsec/configuration) configure the [AppSec Component](/docs/next/appsec/intro) and provide sensible defaults for common web applications.

<ThemedImage
    alt="CrowdSec AppSec configurations"
    sources={{
        light: useBaseUrl("/img/console_appsec_config_light.png"),
        dark: useBaseUrl("/img/console_appsec_config_dark.png"),
    }}
/>

### AppSec Rules Tab

[AppSec Rules](/docs/next/appsec/rules_syntax) are used with the [AppSec Component](/docs/next/appsec/intro) to detect and block web application attacks.

These rules are defined and loaded by [AppSec Configurations](/docs/next/appsec/configuration).

<ThemedImage
    alt="CrowdSec AppSec rules"
    sources={{
        light: useBaseUrl("/img/console_appsec_rules_light.png"),
        dark: useBaseUrl("/img/console_appsec_rules_dark.png"),
    }}
/>

## Next steps

Now that you have explored the Hub, return to the [post-installation steps](/getting_started/next_steps.mdx#1-crowdsec-console-) to continue setup.
